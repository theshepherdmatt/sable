# Sable's PURE-PYTHON deps -- installed by pip (see install.sh).
#
# The compiled/C-extension deps are NOT here: install.sh apt-installs them as
# prebuilt Debian packages (python3-pil = Pillow, python3-cbor2, python3-spidev,
# python3-rpi.gpio, python3-smbus) so NOTHING needs a compiler/Rust on 32-bit ARM.
# luma.core pulls those, but they are already satisfied by apt, so pip just adds
# the pure-Python wheels below.
#
# EIO3 LOAD-BEARING PAIR (Volumio's Socket.IO) -- do not bump (see DESIGN.md).
luma.oled==3.8.0
luma.core==2.3.1
python-socketio==4.6.1
python-engineio==3.14.2
smbus2==0.5.0

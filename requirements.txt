# Sable runtime deps -- curated, only what is actually imported.
# Pinned to the versions Volumio 4 / Bookworm / Python 3.11 already ships,
# so a fresh install needs no compilation.

# OLED (SSD1322 over SPI)
luma.oled==3.13.0
luma.core==2.4.2
Pillow==9.5.0

# Volumio Socket.IO -- EIO3 LOAD-BEARING PAIR, do not bump (see DESIGN.md).
python-socketio==4.6.1
python-engineio==3.14.2

# Hardware I/O
smbus2==0.5.0          # MCP23017 (i2c-1)
# RPi.GPIO is installed via apt (python3-rpi.gpio), not pip.
